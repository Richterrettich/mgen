require 'spec_helper'

describe spring-gen do
  it 'has a version number' do
    expect(spring-gen::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
